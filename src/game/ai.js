// IA do modo VS Máquina. STUB no 1º Tempo (retorna 0 = parado).
// Implementação completa na Prorrogação: segue X da bola, 85% quando a bola vem,
// 40% quando vai, dead zone 8px, 8% de hesitação por frame.

// Retorna input lateral -1..1 para o goleiro controlado pela máquina (lado 'top').
export function aiAxis(/* state */) {
  return 0;
}
